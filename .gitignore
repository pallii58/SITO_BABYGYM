# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
*.swp
*.swo
*~
.idea/

# Env e segreti locali (mai in repo)
.env
.env.*
!.env.example

# Log e backup
*.log
*.bak
*.tmp

# Stato deploy FTP (solo runner / path custom su server; non nel tema)
.ftp-deploy-sync-state.json
**/.ftp-deploy-sync-state.json
.ftp-deploy-babygym-sync-state.json

# Dipendenze (se aggiungi build tool in futuro)
node_modules/
vendor/
